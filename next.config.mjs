/** @type {import('next').NextConfig} */
const nextConfig = {
  env: {
    SECRET_KEY: 'secrets',
    SECRET_PASS: 'mandre361@west-mec.org',
    SECRET_EMAIL: '1234',
  },
};

export default nextConfig;
